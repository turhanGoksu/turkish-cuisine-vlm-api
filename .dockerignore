# Keep the build context small: these files are never needed inside the image.
.git/
.gitignore
__pycache__/
*.py[cod]
.venv/
venv/
.env
.cache/
models/
.DS_Store
.idea/
.vscode/
README.md
# CI definitions run on GitHub, never inside the image.
.github/
